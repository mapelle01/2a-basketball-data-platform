[tool.pytest.ini_options]
minversion = "6.0"
testpaths = ["tests"]
pythonpath = ["src", "tests/api", "scripts/validation"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
