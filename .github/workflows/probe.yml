name: probe2
on:
  workflow_dispatch:
  push:
    branches: [wf-probe]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo hi
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - run: echo deploy-skipped
