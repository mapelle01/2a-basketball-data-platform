# FASE 11 + FASE 12 — Runtime dependencies of feb_score.
# FEB source connector dependencies are included so CI can exercise its parser
# and the production image can run the connector without a separate environment.
jsonschema>=4.0
fastapi>=0.110
uvicorn[standard]>=0.29
psycopg[binary]>=3.1
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
